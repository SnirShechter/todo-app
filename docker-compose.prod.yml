version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: todos
      POSTGRES_USER: todos
      POSTGRES_PASSWORD: todos123
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U todos"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    image: ghcr.io/snirshechter/todo-app-backend:latest
    environment:
      DATABASE_URL: postgresql://todos:todos123@postgres:5432/todos
      PORT: "3001"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: ghcr.io/snirshechter/todo-app-frontend:latest
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  pgdata:
